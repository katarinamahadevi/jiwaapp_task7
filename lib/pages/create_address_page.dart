import 'package:flutter/material.dart';

class CreateAddressPage extends StatelessWidget {
  const CreateAddressPage({super.key}); //lokasimu saat ini

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}